version: 2
sql:
  - engine: "sqlite"
    schema: "storage/datastore/migrations"
    queries: "storage/datastore/queries.sql"
    gen:
      go:
        package: "repo"
        out: "storage/datastore/repo"
